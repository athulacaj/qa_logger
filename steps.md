### steps to run before publishing the code.

1. Run the following command to install the required packages:
```bash
    ./build_files.sh
```
2. Run the following command to build the project:
```bash
    dart format .
```
